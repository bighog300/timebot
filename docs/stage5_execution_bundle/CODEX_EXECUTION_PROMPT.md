You are Codex operating on the repo.

Goal: Complete Stage 5 (production hardening, QA, performance, deployment readiness).

Steps:
1. Audit frontend + backend build/test failures
2. Fix dependency + environment issues
3. Add missing tests (backend + frontend)
4. Validate E2E flows
5. Improve performance (API + UI)
6. Add error handling + edge cases
7. Ensure production build + deploy scripts work

Rules:
- Do not rewrite architecture
- Fix incrementally
- Prefer small commits
- Add tests alongside fixes
